{
  "name": "@ownhealthil/data-generator",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/*",
    "!dist/**/*.test.*"
  ],
  "scripts": {
    "compile": "tsc -p .",
    "prepare": "npm run test && npm run compile",
    "prepublishOnly": "npm run clean",
    "test": "jest"
  },
  "dependencies": {
    "bluebird": "^3.7.2",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@types/bluebird": "^3.5.36",
    "@types/jest": "^28.1.6",
    "@types/lodash": "^4.14.182"
  }
}
